{
  "name": "@yunzhi/utils",
  "version": "0.0.4",
  "description": "utils for yunzhi",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "private": false,
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "scripts": {
      "test": "jasmine"
    }
  },
  "devDependencies": {
    "@types/jasmine": "^3.9.1",
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "jasmine": "^3.9.0",
    "jasmine-ts-console-reporter": "^3.1.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "sweetalert2": "^11.0.19"
  }
}
