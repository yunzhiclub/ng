/**
 * 等待方法体中的函数返回true后，再返回promise
 * @param lastMethod 返回boolean值的函数
 * @param failureMessage 错误提示消息
 * @param timeout 超时时间
 */
export declare const waitsFor: (lastMethod: () => boolean, failureMessage?: string, timeout?: number) => Promise<boolean>;
/**
 * 等待一会
 * @param timeout 中断时长
 */
export declare const waits: (timeout?: number) => Promise<void>;
